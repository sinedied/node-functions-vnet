# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: test-function-vnet

services:
  webapp:
    project: ./swa
    language: js
    host: staticwebapp

  api:
    project: ./api
    language: ts
    host: function
